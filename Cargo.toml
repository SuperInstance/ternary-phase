[package]
description = "ternary-phase - SuperInstance ecosystem crate"
repository = "https://github.com/SuperInstance/ternary-phase"
name = "ternary-phase"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
name = "ternary_phase"
path = "src/lib.rs"
